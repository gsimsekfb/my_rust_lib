{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "test-all",
            "type": "shell",
            "command": "cargo test --workspace --all-targets -j 6"
            // "command": "cargo test --workspace --all-targets --all-features -j 6"
        },
        {
            "label": "clippy",
            "type": "shell",
            "command": "cargo clippy --workspace --all-targets -j 6"
            // "command": "cargo clippy --workspace --all-targets --all-features -j 6"
        },
    ]
}


// todo
// - unpack change middle elem of size 5 arr
// - unpack first/last elem of size 3 arr

#[test]
fn f1() {

    let arr = [1, 2, 3, 4, 5];
    let [_, _, mid, _, _] = arr;  // for a 5-element array
    assert_eq!(mid, 3);
    
    // todo
    // .. can be used once


}